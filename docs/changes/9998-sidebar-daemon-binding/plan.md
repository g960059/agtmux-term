# Plan

1. Reproduce the missing provider/activity binding and confirm the overlay drop
   point.
2. Update the local metadata overlay replacement rule for same-pane-instance
   shell to agent promotion.
3. Restore the managed-row visual contract for provider badge, ring state, and
   trailing freshness once daemon truth is present.
4. Add regression tests for the overlay cache, sidebar-facing display state,
   and fake-daemon rendered row semantics, then run focused validation.
