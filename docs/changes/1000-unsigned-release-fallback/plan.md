# Plan

1. Add release-mode detection and unsigned fallback steps in `release.yml`.
2. Update README and release runbook to describe the temporary unsigned mode.
3. Validate the workflow logic locally, then re-publish `v0.2.0` from the new
   commit and confirm the GitHub Release completes.
