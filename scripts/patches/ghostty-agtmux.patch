diff --git a/include/ghostty.h b/include/ghostty.h
index 40ff55c9b..2b6575269 100644
--- a/include/ghostty.h
+++ b/include/ghostty.h
@@ -461,6 +461,37 @@ typedef struct {
   uint32_t cell_height_px;
 } ghostty_surface_size_s;

+typedef struct {
+  uint64_t precise_event_count;
+  uint64_t precise_step_count;
+  uint64_t precise_first_event_elapsed_ms;
+  uint64_t precise_message_queue_count;
+  uint64_t precise_first_message_queue_elapsed_ms;
+  uint64_t precise_mailbox_notify_count;
+  uint64_t precise_first_mailbox_notify_elapsed_ms;
+  uint64_t precise_write_queue_count;
+  uint64_t precise_write_queue_bytes;
+  uint64_t precise_first_write_queue_elapsed_ms;
+  uint64_t precise_write_completed_count;
+  uint64_t precise_write_completed_bytes;
+  uint64_t precise_first_write_completed_elapsed_ms;
+  uint64_t precise_drain_turn_count;
+  uint64_t precise_drained_message_count;
+  uint64_t precise_drain_requeue_count;
+  uint64_t precise_first_drain_turn_elapsed_ms;
+  uint64_t precise_read_chunk_count;
+  uint64_t precise_read_chunk_bytes;
+  uint64_t precise_read_chunk_max_bytes;
+  uint64_t precise_first_read_chunk_elapsed_ms;
+  uint64_t precise_up_sequence_count;
+  uint64_t precise_down_sequence_count;
+  uint64_t precise_application_cursor_sequence_count;
+  uint64_t precise_normal_cursor_sequence_count;
+  uint64_t precise_read_escape_byte_count;
+  uint64_t precise_read_printable_byte_count;
+  uint64_t precise_read_newline_byte_count;
+} ghostty_surface_alternate_scroll_telemetry_s;
+
 // Config types

 // config.Path
@@ -824,6 +855,13 @@ typedef struct {
   int8_t progress;
 } ghostty_action_progress_report_s;

+// terminal.osc.Command.CustomOSC.C
+typedef struct {
+  uint16_t osc;
+  const uint8_t* payload;
+  uintptr_t len;
+} ghostty_action_custom_osc_s;
+
 // apprt.action.CommandFinished.C
 typedef struct {
   // -1 if no exit code was reported, otherwise 0-255
@@ -921,6 +959,8 @@ typedef enum {
   GHOSTTY_ACTION_SEARCH_SELECTED,
   GHOSTTY_ACTION_READONLY,
   GHOSTTY_ACTION_COPY_TITLE_TO_CLIPBOARD,
+  GHOSTTY_ACTION_RENDERER_FRAME_COMPLETED,
+  GHOSTTY_ACTION_CUSTOM_OSC,
 } ghostty_action_tag_e;

 typedef union {
@@ -962,6 +1002,7 @@ typedef union {
   ghostty_action_search_total_s search_total;
   ghostty_action_search_selected_s search_selected;
   ghostty_action_readonly_e readonly;
+  ghostty_action_custom_osc_s custom_osc;
 } ghostty_action_u;

 typedef struct {
@@ -1080,6 +1121,9 @@ ghostty_surface_config_s ghostty_surface_inherited_config(ghostty_surface_t, gho
 void ghostty_surface_update_config(ghostty_surface_t, ghostty_config_t);
 bool ghostty_surface_needs_confirm_quit(ghostty_surface_t);
 bool ghostty_surface_process_exited(ghostty_surface_t);
+bool ghostty_surface_uses_alternate_scroll(ghostty_surface_t);
+void ghostty_surface_reset_alternate_scroll_telemetry(ghostty_surface_t);
+ghostty_surface_alternate_scroll_telemetry_s ghostty_surface_alternate_scroll_telemetry(ghostty_surface_t);
 void ghostty_surface_refresh(ghostty_surface_t);
 void ghostty_surface_draw(ghostty_surface_t);
 void ghostty_surface_set_content_scale(ghostty_surface_t, double, double);
diff --git a/src/Surface.zig b/src/Surface.zig
index 4d66622e3..1af184d62 100644
--- a/src/Surface.zig
+++ b/src/Surface.zig
@@ -1074,6 +1074,16 @@ pub fn handleMessage(self: *Surface, msg: Message) !void {

         .renderer_health => |health| self.updateRendererHealth(health),

+        .renderer_frame_completed => {
+            _ = self.rt_app.performAction(
+                .{ .surface = self },
+                .renderer_frame_completed,
+                {},
+            ) catch |err| {
+                log.warn("apprt failed to deliver renderer frame completion err={}", .{err});
+            };
+        },
+
         .scrollbar => |scrollbar| self.updateScrollbar(scrollbar),

         .present_surface => try self.presentSurface(),
@@ -1110,6 +1120,21 @@ pub fn handleMessage(self: *Surface, msg: Message) !void {
             try self.selectionScrollTick();
         },

+        .custom_osc => |v| {
+            defer v.req.deinit();
+
+            _ = self.rt_app.performAction(
+                .{ .surface = self },
+                .custom_osc,
+                .{
+                    .osc = v.osc,
+                    .payload = v.req.slice(),
+                },
+            ) catch |err| {
+                log.warn("apprt failed to deliver custom osc err={}", .{err});
+            };
+        },
+
         .start_command => {
             self.command_timer = try .now();
         },
@@ -2416,7 +2441,7 @@ pub fn setFontSize(self: *Surface, size: font.face.DesiredSize) !void {
 /// isn't guaranteed to happen immediately but it will happen as soon as
 /// practical.
 fn queueRender(self: *Surface) !void {
-    try self.renderer_thread.wakeup.notify();
+    try self.renderer_thread.requestRenderAndDraw();
 }

 pub fn sizeCallback(self: *Surface, size: apprt.SurfaceSize) !void {
diff --git a/src/apprt/action.zig b/src/apprt/action.zig
index f6865af83..f18640c2d 100644
--- a/src/apprt/action.zig
+++ b/src/apprt/action.zig
@@ -343,6 +343,12 @@ pub const Action = union(Key) {
     /// otherwise the terminal-set title.
     copy_title_to_clipboard,

+    /// The renderer completed a frame for the target surface.
+    renderer_frame_completed,
+
+    /// Surface-scoped custom OSC carrier payload.
+    custom_osc: terminal.osc.Command.CustomOSC,
+
     /// Sync with: ghostty_action_tag_e
     pub const Key = enum(c_int) {
         quit,
@@ -410,6 +416,8 @@ pub const Action = union(Key) {
         search_selected,
         readonly,
         copy_title_to_clipboard,
+        renderer_frame_completed,
+        custom_osc,

         test "ghostty.h Action.Key" {
             try lib.checkGhosttyHEnum(Key, "GHOSTTY_ACTION_");
diff --git a/src/apprt/embedded.zig b/src/apprt/embedded.zig
index 0d5a4f8da..97654eb57 100644
--- a/src/apprt/embedded.zig
+++ b/src/apprt/embedded.zig
@@ -1281,6 +1281,37 @@ pub const CAPI = struct {
         cell_height_px: u32,
     };

+    const SurfaceAlternateScrollTelemetry = extern struct {
+        precise_event_count: u64,
+        precise_step_count: u64,
+        precise_first_event_elapsed_ms: u64,
+        precise_message_queue_count: u64,
+        precise_first_message_queue_elapsed_ms: u64,
+        precise_mailbox_notify_count: u64,
+        precise_first_mailbox_notify_elapsed_ms: u64,
+        precise_write_queue_count: u64,
+        precise_write_queue_bytes: u64,
+        precise_first_write_queue_elapsed_ms: u64,
+        precise_write_completed_count: u64,
+        precise_write_completed_bytes: u64,
+        precise_first_write_completed_elapsed_ms: u64,
+        precise_drain_turn_count: u64,
+        precise_drained_message_count: u64,
+        precise_drain_requeue_count: u64,
+        precise_first_drain_turn_elapsed_ms: u64,
+        precise_read_chunk_count: u64,
+        precise_read_chunk_bytes: u64,
+        precise_read_chunk_max_bytes: u64,
+        precise_first_read_chunk_elapsed_ms: u64,
+        precise_up_sequence_count: u64,
+        precise_down_sequence_count: u64,
+        precise_application_cursor_sequence_count: u64,
+        precise_normal_cursor_sequence_count: u64,
+        precise_read_escape_byte_count: u64,
+        precise_read_printable_byte_count: u64,
+        precise_read_newline_byte_count: u64,
+    };
+
     // ghostty_clipboard_content_s
     const ClipboardContent = extern struct {
         mime: [*:0]const u8,
@@ -1598,6 +1629,24 @@ pub const CAPI = struct {
         return surface.core_surface.child_exited;
     }

+    export fn ghostty_surface_uses_alternate_scroll(surface: *Surface) bool {
+        const core_surface = &surface.core_surface;
+        core_surface.renderer_state.mutex.lock();
+        defer core_surface.renderer_state.mutex.unlock();
+
+        return core_surface.io.terminal.screens.active_key == .alternate and
+            core_surface.io.terminal.flags.mouse_event == .none and
+            core_surface.io.terminal.modes.get(.mouse_alternate_scroll);
+    }
+
+    export fn ghostty_surface_reset_alternate_scroll_telemetry(_: *Surface) void {}
+
+    export fn ghostty_surface_alternate_scroll_telemetry(
+        _: *Surface,
+    ) SurfaceAlternateScrollTelemetry {
+        return std.mem.zeroes(SurfaceAlternateScrollTelemetry);
+    }
+
     /// Returns true if the surface has a selection.
     export fn ghostty_surface_has_selection(surface: *Surface) bool {
         return surface.core_surface.hasSelection();
diff --git a/src/apprt/gtk/class/application.zig b/src/apprt/gtk/class/application.zig
index 039e853aa..fc35407a3 100644
--- a/src/apprt/gtk/class/application.zig
+++ b/src/apprt/gtk/class/application.zig
@@ -717,6 +717,8 @@ pub const Application = extern struct {

             .open_url => Action.openUrl(self, value),

+            .custom_osc => return Action.customOSC(target, value),
+
             .pwd => Action.pwd(target, value),

             .present_terminal => return Action.presentTerminal(target),
@@ -2338,6 +2340,24 @@ const Action = struct {
         }
     }

+    pub fn customOSC(
+        target: apprt.Target,
+        value: terminal.osc.Command.CustomOSC,
+    ) bool {
+        switch (target) {
+            .app => log.warn(
+                "custom_osc to app is unexpected osc={d} payload_len={d}",
+                .{ value.osc, value.payload.len },
+            ),
+            .surface => log.info(
+                "custom osc ignored in GTK runtime osc={d} payload_len={d}",
+                .{ value.osc, value.payload.len },
+            ),
+        }
+
+        return true;
+    }
+
     pub fn quitTimer(
         self: *Application,
         mode: apprt.action.QuitTimer,
diff --git a/src/apprt/surface.zig b/src/apprt/surface.zig
index 3cb0016fa..30e0012ab 100644
--- a/src/apprt/surface.zig
+++ b/src/apprt/surface.zig
@@ -63,6 +63,9 @@ pub const Message = union(enum) {
     /// Health status change for the renderer.
     renderer_health: renderer.Health,

+    /// The renderer completed a frame for this surface.
+    renderer_frame_completed: void,
+
     /// Tell the surface to present itself to the user. This may require raising
     /// a window and switching tabs.
     present_surface: void,
@@ -85,6 +88,12 @@ pub const Message = union(enum) {
     /// The terminal has reported a change in the working directory.
     pwd_change: WriteReq,

+    /// Surface-scoped custom OSC carrier payload.
+    custom_osc: struct {
+        osc: u16,
+        req: WriteReq,
+    },
+
     /// The terminal encountered a bell character.
     ring_bell,

diff --git a/src/renderer/Thread.zig b/src/renderer/Thread.zig
index 508721379..bfe21eeae 100644
--- a/src/renderer/Thread.zig
+++ b/src/renderer/Thread.zig
@@ -67,6 +67,10 @@ draw_active: bool = false,
 draw_now: xev.Async,
 draw_now_c: xev.Completion = .{},

+/// When set, the next wakeup-triggered render should also force an immediate
+/// draw on the renderer thread after frame data is refreshed.
+draw_after_render_requested: std.atomic.Value(u8) = .{ .raw = 0 },
+
 /// The timer used for cursor blinking
 cursor_h: xev.Timer,
 cursor_c: xev.Completion = .{},
@@ -472,6 +476,10 @@ fn drainMailbox(self: *Thread) !void {
                 self.renderer.search_matches_dirty = true;
             },

+            .render_and_draw => {
+                _ = self.draw_after_render_requested.swap(1, .seq_cst);
+            },
+
             .inspector => |v| {
                 self.flags.has_inspector = v;
             },
@@ -489,6 +497,13 @@ fn changeConfig(self: *Thread, config: *const DerivedConfig) !void {
     self.config = config.*;
 }

+/// Request a renderer-thread-owned render immediately followed by an
+/// immediate draw pass after the frame data is refreshed.
+pub fn requestRenderAndDraw(self: *Thread) !void {
+    _ = self.draw_after_render_requested.swap(1, .seq_cst);
+    try self.wakeup.notify();
+}
+
 /// Trigger a draw. This will not update frame data or anything, it will
 /// just trigger a draw/paint.
 fn drawFrame(self: *Thread, now: bool) void {
@@ -528,9 +543,18 @@ fn wakeupCallback(
     t.drainMailbox() catch |err|
         log.err("error draining mailbox err={}", .{err});

+    const draw_after_render = t.draw_after_render_requested.swap(0, .seq_cst) != 0;
+
     // Render immediately
     _ = renderCallback(t, undefined, undefined, {});

+    // Immediate renderer-thread draws are reserved for explicit host seams
+    // such as input/scroll recovery. They still stay renderer-owned because
+    // the render thread performs both the frame update and the follow-on draw.
+    if (draw_after_render and t.renderer.hasVsync()) {
+        t.drawFrame(true);
+    }
+
     // The below is not used anymore but if we ever want to introduce
     // a configuration to introduce a delay to coalesce renders, we can
     // use this.
diff --git a/src/renderer/generic.zig b/src/renderer/generic.zig
index ff632f64a..e41569107 100644
--- a/src/renderer/generic.zig
+++ b/src/renderer/generic.zig
@@ -1725,6 +1725,10 @@ pub fn Renderer(comptime GraphicsAPI: type) type {
             self: *Self,
             health: Health,
         ) void {
+            _ = self.surface_mailbox.push(.{
+                .renderer_frame_completed = {},
+            }, .{ .instant = {} });
+
             // If our health value hasn't changed, then we do nothing. We don't
             // do a cmpxchg here because strict atomicity isn't important.
             if (self.health.load(.seq_cst) != health) {
diff --git a/src/renderer/message.zig b/src/renderer/message.zig
index a47b96080..ea46ddb72 100644
--- a/src/renderer/message.zig
+++ b/src/renderer/message.zig
@@ -61,6 +61,11 @@ pub const Message = union(enum) {
     /// no match currently.
     search_selected_match: ?SearchMatch,

+    /// Force the next wakeup-triggered render to follow with an immediate
+    /// draw. This stays renderer-owned while avoiding visible clumping on
+    /// narrow seams such as input/scroll recovery.
+    render_and_draw: void,
+
     /// Activate or deactivate the inspector.
     inspector: bool,

diff --git a/src/terminal/osc.zig b/src/terminal/osc.zig
index 0086e48bc..f11edc56c 100644
--- a/src/terminal/osc.zig
+++ b/src/terminal/osc.zig
@@ -22,6 +22,8 @@ pub const semantic_prompt = parsers.semantic_prompt;

 const log = std.log.scoped(.osc);

+pub const AGTMUX_BRIDGE_OSC: u16 = 9911;
+
 pub const Command = union(Key) {
     /// This generally shouldn't ever be set except as an initial zero value.
     /// Ignore it.
@@ -64,6 +66,9 @@ pub const Command = union(Key) {
         value: [:0]const u8,
     },

+    /// Raw custom OSC carrier. This is currently used for the agtmux bridge.
+    custom_osc: CustomOSC,
+
     /// OSC 22. Set the mouse shape. There doesn't seem to be a standard
     /// naming scheme for cursors but it looks like terminals such as Foot
     /// are moving towards using the W3C CSS cursor names. For OSC parsing,
@@ -174,6 +179,7 @@ pub const Command = union(Key) {
             "semantic_prompt",
             "clipboard_contents",
             "report_pwd",
+            "custom_osc",
             "mouse_shape",
             "color_operation",
             "kitty_color_protocol",
@@ -230,6 +236,39 @@ pub const Command = union(Key) {
         }
     };

+    pub const CustomOSC = struct {
+        osc: u16,
+        payload: []const u8,
+
+        // Sync with: ghostty_action_custom_osc_s
+        pub const C = extern struct {
+            osc: u16,
+            payload: [*]const u8,
+            len: usize,
+        };
+
+        pub fn cval(self: CustomOSC) C {
+            return .{
+                .osc = self.osc,
+                .payload = self.payload.ptr,
+                .len = self.payload.len,
+            };
+        }
+
+        pub fn format(
+            value: CustomOSC,
+            comptime _: []const u8,
+            _: std.fmt.FormatOptions,
+            writer: anytype,
+        ) !void {
+            try writer.print("{s}{{ osc: {d}, len: {d} }}", .{
+                @typeName(CustomOSC),
+                value.osc,
+                value.payload.len,
+            });
+        }
+    };
+
     comptime {
         assert(@sizeOf(Command) == switch (@sizeOf(usize)) {
             4 => 44,
@@ -360,6 +399,9 @@ pub const Parser = struct {
         @"133",
         @"552",
         @"777",
+        @"99",
+        @"991",
+        @"9911",
         @"1337",
         @"5522",
     };
@@ -415,6 +457,7 @@ pub const Parser = struct {
             .conemu_sleep,
             .conemu_wait_input,
             .conemu_xterm_emulation,
+            .custom_osc,
             .hyperlink_end,
             .hyperlink_start,
             .invalid,
@@ -663,6 +706,21 @@ pub const Parser = struct {
                 else => self.state = .invalid,
             },

+            .@"99" => switch (c) {
+                '1' => self.state = .@"991",
+                else => self.state = .invalid,
+            },
+
+            .@"991" => switch (c) {
+                '1' => self.state = .@"9911",
+                else => self.state = .invalid,
+            },
+
+            .@"9911" => switch (c) {
+                ';' => self.writeToAllocating(),
+                else => self.state = .invalid,
+            },
+
             .@"5522",
             => switch (c) {
                 ';' => self.writeToAllocating(),
@@ -675,6 +733,7 @@ pub const Parser = struct {
             .@"8",
             .@"9",
             => switch (c) {
+                '9' => self.state = .@"99",
                 ';' => self.writeToFixed(),
                 else => self.state = .invalid,
             },
@@ -758,6 +817,25 @@ pub const Parser = struct {

             .@"777" => parsers.rxvt_extension.parse(self, terminator_ch),

+            .@"99",
+            .@"991",
+            => null,
+
+            .@"9911" => custom_osc: {
+                const writer = self.writer orelse {
+                    self.state = .invalid;
+                    break :custom_osc null;
+                };
+                const data = writer.buffered();
+                self.command = .{
+                    .custom_osc = .{
+                        .osc = AGTMUX_BRIDGE_OSC,
+                        .payload = data,
+                    },
+                };
+                break :custom_osc &self.command;
+            },
+
             .@"1337" => parsers.iterm2.parse(self, terminator_ch),

             .@"5522" => parsers.kitty_clipboard_protocol.parse(self, terminator_ch),
@@ -769,3 +847,37 @@ test {
     _ = parsers;
     _ = encoding;
 }
+
+test "OSC: custom osc 9911 preserves payload" {
+    const testing = std.testing;
+
+    var p: Parser = .init(null);
+    for ("9911;agtmux;open=/tmp/demo.md") |c| p.next(c);
+
+    const cmd = p.end(0x07).?.*;
+    try testing.expect(cmd == .custom_osc);
+    try testing.expectEqual(AGTMUX_BRIDGE_OSC, cmd.custom_osc.osc);
+    try testing.expectEqualStrings(
+        "agtmux;open=/tmp/demo.md",
+        cmd.custom_osc.payload,
+    );
+}
+
+test "OSC: custom osc 9911 preserves long payloads" {
+    const testing = std.testing;
+
+    var p: Parser = .init(testing.allocator);
+    defer p.deinit();
+
+    const payload = try testing.allocator.alloc(u8, Parser.MAX_BUF + 64);
+    defer testing.allocator.free(payload);
+    @memset(payload, 'x');
+
+    for ("9911;") |c| p.next(c);
+    for (payload) |c| p.next(c);
+
+    const cmd = p.end(0x07).?.*;
+    try testing.expect(cmd == .custom_osc);
+    try testing.expectEqual(AGTMUX_BRIDGE_OSC, cmd.custom_osc.osc);
+    try testing.expectEqualSlices(u8, payload, cmd.custom_osc.payload);
+}
diff --git a/src/terminal/stream.zig b/src/terminal/stream.zig
index e89c73e66..c3f5875c8 100644
--- a/src/terminal/stream.zig
+++ b/src/terminal/stream.zig
@@ -1996,6 +1996,13 @@ pub fn Stream(comptime Handler: type) type {
                     try self.handler.vt(.report_pwd, .{ .url = v.value });
                 },

+                .custom_osc => |v| {
+                    if (@hasDecl(T, "customOSC")) {
+                        try self.handler.customOSC(v);
+                        return;
+                    } else log.warn("unimplemented OSC callback: {}", .{cmd});
+                },
+
                 .mouse_shape => |v| {
                     const shape = MouseShape.fromString(v.value) orelse {
                         @branchHint(.unlikely);
diff --git a/src/termio/stream_handler.zig b/src/termio/stream_handler.zig
index 8c1b5b8ab..06022476b 100644
--- a/src/termio/stream_handler.zig
+++ b/src/termio/stream_handler.zig
@@ -102,6 +102,9 @@ pub const StreamHandler = struct {
     /// isn't guaranteed to happen immediately but it will happen as soon as
     /// practical.
     pub inline fn queueRender(self: *StreamHandler) !void {
+        if (self.renderer_mailbox.push(.{ .render_and_draw = {} }, .{ .instant = {} }) == 0) {
+            _ = self.renderer_mailbox.push(.{ .render_and_draw = {} }, .{ .forever = {} });
+        }
         try self.renderer_wakeup.notify();
     }

@@ -1424,6 +1427,19 @@ pub const StreamHandler = struct {
         }
     }

+    pub fn customOSC(
+        self: *StreamHandler,
+        osc_payload: terminal.osc.Command.CustomOSC,
+    ) !void {
+        self.surfaceMessageWriter(.{ .custom_osc = .{
+            .osc = osc_payload.osc,
+            .req = try apprt.surface.Message.WriteReq.init(
+                self.alloc,
+                osc_payload.payload,
+            ),
+        } });
+    }
+
     fn showDesktopNotification(
         self: *StreamHandler,
         title: []const u8,
diff --git a/pkg/macos/video/display_link.zig b/pkg/macos/video/display_link.zig
index 4bbf58a0c..9f624e30c 100644
--- a/pkg/macos/video/display_link.zig
+++ b/pkg/macos/video/display_link.zig
@@ -8,12 +8,16 @@ pub const DisplayLink = opaque {
         InvalidOperation,
     };

-    pub fn createWithActiveCGDisplays() Allocator.Error!*DisplayLink {
+    pub const CreateError = Allocator.Error || Error;
+
+    pub fn createWithActiveCGDisplays() CreateError!*DisplayLink {
         var result: ?*DisplayLink = null;
-        if (c.CVDisplayLinkCreateWithActiveCGDisplays(
+        const status = c.CVDisplayLinkCreateWithActiveCGDisplays(
             @ptrCast(&result),
-        ) != c.kCVReturnSuccess)
-            return error.OutOfMemory;
+        );
+        if (status != c.kCVReturnSuccess) {
+            return error.InvalidOperation;
+        }

         return result orelse error.OutOfMemory;
     }
diff --git a/src/renderer/generic.zig b/src/renderer/generic.zig
index 02897f508..01fc0b06c 100644
--- a/src/renderer/generic.zig
+++ b/src/renderer/generic.zig
@@ -251,6 +251,9 @@ pub fn Renderer(comptime GraphicsAPI: type) type {
         /// don't support a display link.
         display_link: ?DisplayLink = null,

+        /// Cached render-thread wakeup target for display-link callbacks.
+        display_link_draw_now: ?*xev.Async = null,
+
         /// Health of the most recently completed frame.
         health: std.atomic.Value(Health) = .{ .raw = .healthy },

@@ -746,7 +749,10 @@ pub fn Renderer(comptime GraphicsAPI: type) type {

             const display_link: ?DisplayLink = switch (builtin.os.tag) {
                 .macos => if (options.config.vsync)
-                    try macos.video.DisplayLink.createWithActiveCGDisplays()
+                    macos.video.DisplayLink.createWithActiveCGDisplays() catch |err| display_link: {
+                        log.warn("display link unavailable during renderer init err={}", .{err});
+                        break :display_link null;
+                    }
                 else
                     null,
                 else => null,
@@ -824,6 +830,7 @@ pub fn Renderer(comptime GraphicsAPI: type) type {
                 .api = api,
                 .swap_chain = swap_chain,
                 .display_link = display_link,
+                .display_link_draw_now = null,
             };

             try result.initShaders();
@@ -945,6 +952,7 @@ pub fn Renderer(comptime GraphicsAPI: type) type {

             // If we don't support a display link we have no work to do.
             if (comptime DisplayLink == void) return;
+            self.display_link_draw_now = &thr.draw_now;

             // This is when we know our "self" pointer is stable so we can
             // setup the display link. To setup the display link we set our
@@ -967,6 +975,7 @@ pub fn Renderer(comptime GraphicsAPI: type) type {

             // If we don't support a display link we have no work to do.
             if (comptime DisplayLink == void) return;
+            self.display_link_draw_now = null;

             // Stop our display link. If this fails its okay it just means
             // that we either never started it or the view its attached to
@@ -1048,11 +1057,32 @@ pub fn Renderer(comptime GraphicsAPI: type) type {
         /// Called when we get an updated display ID for our display link.
         pub fn setMacOSDisplayID(self: *Self, id: u32) !void {
             if (comptime DisplayLink == void) return;
+            if (self.display_link == null) {
+                self.display_link = macos.video.DisplayLink.createWithActiveCGDisplays() catch |err| create: {
+                    log.warn("display link unavailable while applying display id={} err={}", .{ id, err });
+                    break :create null;
+                };
+                if (self.display_link) |display_link| {
+                    if (self.display_link_draw_now) |draw_now| {
+                        display_link.setOutputCallback(
+                            xev.Async,
+                            &displayLinkCallback,
+                            draw_now,
+                        ) catch |err| {
+                            log.warn("error setting late display link callback err={}", .{err});
+                        };
+                    }
+                }
+            }
+
             const display_link = self.display_link orelse return;
             log.info("updating display link display id={}", .{id});
             display_link.setCurrentCGDisplay(id) catch |err| {
                 log.warn("error setting display link display id err={}", .{err});
             };
+            if (self.display_link_draw_now != null) {
+                display_link.start() catch {};
+            }
         }

         /// True if our renderer has animations so that a higher frequency
