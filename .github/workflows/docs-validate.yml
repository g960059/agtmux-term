name: Docs Validate

on:
  push:
    branches: [main]
  pull_request:
  merge_group:

jobs:
  docs-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate docs layout
        shell: bash
        run: scripts/dev/validate-docs.sh
